#include <stdio.h>

/*
    Aplicatia 11.3: Definim reducerea unei matrici cu elemente intregi ca fiind valoarea calculata
    astfel: daca matricea nu are niciun element, reducerea este 0. Daca matricea are un element,
    reducerea este valoarea acelui element. 
    Pentru orice alte cazuri, matricea se subimparte in 4 matrici prin taierea ei in cruce, prin
    mijlocul matricii. Reducerea va fi maximul reducerilor celor doua matrici superioare, minus
    minimul reducerilor celor doua matrici inferioare. Sa se calculeze reducerea unei matrici de
    dimensiuni m,n citita de la tastatura.

    divide et impera
*/

#define SIZE_MAX 100

int main() {

    return 0;
}